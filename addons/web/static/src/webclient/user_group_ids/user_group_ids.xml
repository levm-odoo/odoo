<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

<t t-name="web.ResUserGroups">
    <div class="o_group row align-items-start">
        <div t-foreach="sections" t-as="section" t-key="section.id" class="o_inner_group grid col-lg-6">
            <div class="g-col-sm-2">
                <div class="o_horizontal_separator mt-4 mb-3 text-uppercase fw-bolder small" t-out="section.name"/>
            </div>
            <t t-foreach="section.categories" t-as="category" t-key="category.id">
                <div class="o_cell o_wrap_label text-break text-900">
                    <label class="o_form_label" t-attf-for="category_{{category.id}}">
                        <t t-set="tooltipInfo" t-value="getTooltipInfo(category)"/>
                        <t t-esc="category.name"/><t t-call="web.FormLabelHelp" t-if="tooltipInfo"/>
                    </label>
                </div>
                <div class="o_cell o_wrap_input text-break mb-3 mb-sm-0">
                    <div class="o_field_widget o_field_selection">
                        <select class="o_input pe-3"
                                t-on-change="onChange"
                                t-att-data-category-id="category.id"
                                t-attf-id="category_{{category.id}}">
                            <t t-set="value" t-value="getSelectedGroup(category)"/>
                            <option class="text-black" t-att-selected="value === false"/>
                            <option t-foreach="category.groups" t-as="option" t-key="option[0]"
                                class="text-black"
                                t-att-selected="option[0] === value"
                                t-att-value="option[0]"
                                t-esc="option[1]"
                            />
                        </select>
                    </div>
                </div>
            </t>
        </div>
    </div>
</t>

</templates>
