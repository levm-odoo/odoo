<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">Estate.Property.offers</field>
        <field name="model">estate.property.offers</field>
        <field name="arch" type="xml">
            <list string="channel" editable="bottom" decoration-danger='status == "refused"' decoration-success='status == "accepted"' >
                <field name="price"/>
                <field name="partner_id"/>
                <field name="status"/>
                <button name="action_accepted" type="object" title='accept' icon="fa-check" invisible="status in ['accepted','refused']"/>
                <button name="action_refused" type="object" title='refuse' icon="fa-remove" invisible="status in ['accepted','refused']"/>
                <field name="validity"/>
                <field name="date_deadline"/>
            </list>
        </field>
    </record>
    <record id="estate_property_offers_form" model="ir.ui.view">
        <field name="name">estate.property.offers.form</field>
        <field name="model">estate.property.offers</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="status"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.property.offers</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>
</odoo>