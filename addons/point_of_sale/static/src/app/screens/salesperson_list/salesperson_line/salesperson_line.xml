<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="point_of_sale.SalespersonLine">
        <!-- Mobile view -->
        <t t-if="ui.isSmall">
            <div class="salesperson-info d-flex flex-column p-1 border-bottom" t-att-class="{'bg-primary-subtle': props.isSelected}" t-att-data-id="props.salesperson.id"
                t-on-click="() => this.props.onClickSalesperson(props.salesperson)">
                <div class="d-flex justify-content-between align-items-center p-1">
                    <div>
                        <b t-esc="props.salesperson.name or ''" />
                        <div class="role-field text-bg-muted" t-esc="props.salesperson.role or ''" />
                    </div>
                    <Dropdown>
                        <button class="btn btn-light fa fa-bars" />
                        <t t-set-slot="content">
                            <DropdownItem onSelected="() => props.onClickEdit(props.salesperson)">
                                Edit Details
                            </DropdownItem>
                            <DropdownItem onSelected="() => props.onClickOrders(props.salesperson)">
                                All Orders
                            </DropdownItem>
                        </t>
                    </Dropdown>
                </div>
                <div class="salesperson-line-email p-1">
                    <div class="mb-1" t-if="props.salesperson.phone">
                        <i class="fa fa-fw fa-phone me-2" /><t t-esc="props.salesperson.phone" />
                    </div>
                    <div class="mb-1" t-if="props.salesperson.mobile">
                        <i class="fa fa-fw fa-mobile me-2" /><t t-esc="props.salesperson.mobile" />
                    </div>
                    <div t-if="props.salesperson.email" class="email-field mb-1">
                        <i class="fa fa-fw fa-paper-plane-o me-2" /><t t-esc="props.salesperson.email" />
                    </div>
                </div>
                <div class="d-flex justify-content-between align-items-center p-1">
                    <button t-if="props.isSelected" t-on-click.stop="props.onClickUnselect" class="unselect-tag-mobile d-inline-block d-lg-none btn btn-light border ms-2">
                        <i class="fa fa-times me-1"></i>
                        <span>UNSELECT</span>
                    </button>
                </div>
            </div>
        </t>

        <!-- Desktop view -->
        <t t-else="">
            <tr class="salesperson-line salesperson-info" t-att-class="{'selected': props.isSelected}" t-att-data-id="props.salesperson.id"
                t-on-click="() => this.props.onClickSalesperson(props.salesperson)">
                <td>
                    <b t-esc="props.salesperson.name or ''" />
                    <div class="role-field text-bg-muted" t-esc="props.salesperson.role or ''" />
                </td>
                <td class="salesperson-line-email">
                    <div t-if="props.salesperson.phone">
                        <i class="fa fa-fw fa-phone me-2"/><t t-esc="props.salesperson.phone"/>
                    </div>
                    <div t-if="props.salesperson.mobile">
                        <i class="fa fa-fw fa-mobile me-2"/><t t-esc="props.salesperson.mobile"/>
                    </div>
                    <div t-if="props.salesperson.email" class="email-field">
                        <i class="fa fa-fw fa-paper-plane-o me-2"/><t t-esc="props.salesperson.email" />
                    </div>
                </td>
                <td class="edit-salesperson-button-cell align-middle pe-0">
                    <button t-if="props.isSelected" t-on-click.stop="props.onClickUnselect" class="unselect-tag d-lg-inline-block d-none btn btn-link btn-lg mt-1 float-end">
                        <i class="fa fa-check"/>
                    </button>
                </td>
                <td class="edit-salesperson-button-cell align-middle">
                    <Dropdown>
                        <button class="btn btn-light btn-lg lh-lg border float-end">
                            <i class="fa fa-fw fa-bars"/>
                        </button>
                        <t t-set-slot="content">
                            <DropdownItem onSelected="() => props.onClickEdit(props.salesperson)">
                                Edit Details
                            </DropdownItem>
                            <DropdownItem onSelected="() => props.onClickOrders(props.salesperson)">
                                All Orders
                            </DropdownItem>
                        </t>
                    </Dropdown>
                </td>
            </tr>
        </t>
    </t>

</templates>
